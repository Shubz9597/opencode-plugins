---
description: Print a compact token/cost table for all sessions (input, output, cost)
---

Call the `cost` tool. Reply with ONLY the tool's output and nothing else — no
introductions, explanations, or summaries:

1. The main table and everything up to (but not including) the "TSV" line, in
   one fenced code block.
2. The TSV section (the "TSV" line and everything after it) in a second fenced
   code block, verbatim — the user copies that one into Excel.
